# RecoverSense — Contribution Rules

AI-generated code is not automatically trusted code.

Before accepting generated code:
- inspect it
- understand it
- test it
- remove unnecessary abstractions
- verify financial safety
- check it against the architecture

Do not merge code solely because it compiles.
